import { DashboardLayout } from "./DashboardLayout.jsx";

// Legacy dealer routes share the same portal shell as the active finance routes.
export default function DealerLayout() {
  return <DashboardLayout />;
}
